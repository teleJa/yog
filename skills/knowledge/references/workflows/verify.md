# Verify Workflow

Resolve `repoRoot` and `knowledgeRoot`, run internal `verify.mjs`, and report `check-index` and lint results without writing. Classify failures as stale index, structural issue, missing file, invalid relationship, empty content, or quality issue when evidence allows. Never claim pass without the actual command result.
