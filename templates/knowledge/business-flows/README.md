# Business Flows

Business flow documents describe one end-to-end business operation and connect the contexts that participate in it.

Use a business flow when a user needs the whole story: entry points, actors, state changes, participating systems, context reading order, and open questions. Keep detailed boundaries in `contexts/*` and implementation facts in `evidence/*`.
